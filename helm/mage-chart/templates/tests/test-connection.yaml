apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mage-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mage-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mage-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
