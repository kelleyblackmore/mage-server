apiVersion: v1
kind: Service
metadata:
  name: {{ include "mage-chart.fullname" . }}
  labels:
    {{- include "mage-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mage-chart.selectorLabels" . | nindent 4 }}
