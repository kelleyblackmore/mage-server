apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-idp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "auth-idp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-idp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
